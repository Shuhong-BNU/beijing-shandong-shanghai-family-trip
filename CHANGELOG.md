# CHANGELOG

## v1.3.3 — 2026-08-08 16:55 (UTC+8)

- 新增 `/v133/` 独立版本入口；根入口切换到 v1.3.3，`/v132/`、`/v131/`、`/v130/` 继续保留为历史固定入口。
- A–E 五个方案在“方案速览”中各新增一张高德全程总路线图；每张图独立可折叠，只有展开后才创建 AMap 实例并绘制路线。
- “执行行程”顶部为当前选中方案新增同款全程总路线图，和每天局部地图形成“全局总览 + 单日细节”两层结构。
- 总路线图聚合该方案 8 天全部地理节点；重复到访同一地点合并为一个 Marker，并在详标中并列显示全部 Day / 日期信息，避免同坐标 Marker 完全重叠。
- 总路线图节点详标默认直接显示在对应地理节点旁，包含唯一节点编号、Day、第几天日期和节点名称；拖动、缩放、容器变化后重新计算标签偏移，尽量避免邻近名称互相遮挡。
- 总路线图相邻路线段使用高对比配色，路线图例按 `D几 + 日期 + 段序 + 交通方式` 展示；使用高德 Polyline `showDir` 方向箭头表示实际行进方向。
- 飞机、铁路、步行、轮渡等特殊/长途段在总图中使用虚线；没有冻结 polyline 的长途航段使用节点间直线作为全局空间关系表达，不重新调用 Web Service 算路。
- Day 8 明确补入虹桥后的双分支：爸妈虹桥→赣州、女友虹桥→浦东→广州，使每个方案的全程总图覆盖最终分流节点。
- v1.3.3 仅新增 UI / 地图聚合层，继续复用 `assets/data/route-metrics-v131.json`，未重新冻结 60 段路线数据。

## v1.3.2 — 2026-08-08 16:27 (UTC+8)

- 新增 `/v132/` 独立版本入口；根入口切换到 v1.3.2，`/v131/` 与 `/v130/` 继续保留为历史固定入口。
- “显示节点名称”改为真正的地图地理标签：名称直接显示在对应节点旁，而不是地图顶部浮动清单。
- 节点名称与经纬度 Marker 绑定；地图缩放/拖动后自动跟随，并在 `zoomchange / moveend / resize` 时重新计算标签位置。
- 增加轻量标签碰撞规避：优先在节点四周寻找无遮挡位置，密集节点再扩大候选半径，尽量做到全部名称可见且不互相压住。
- 保留 v1.3.1 的“地图默认折叠 + 展开后才加载”、按行程阶段着色和阶段图例。
- 重做逐餐“真实图”：每一餐都由确定的真实代表图填充，不再依赖运行时 Commons 搜索成功与否；相似餐次允许复用同类代表图，并明确图片不代表最终餐厅实物。
- 逐餐图片继续使用懒加载与异步解码；单图失败时回退到固定真实中餐图，避免空白单元格。

## v1.3.1 — 2026-08-08 16:11 (UTC+8)

- 新增 `/v131/` 独立版本入口；根入口切换到 v1.3.1，`/v130/` 保留为 v1.3.0 历史页面。
- 高德地图生产环境正式使用公开 JS API Key + Cloudflare Worker `serviceHost`；`securityJsCode` 只保存在 Worker Secret，不进入公开仓库或浏览器。
- 每日地图改为二级折叠，默认不创建 AMap 实例；用户展开地图后才加载底图和路线，减少首屏资源消耗。
- 地图路线由“按交通方式着色”改为“按当天行程阶段着色”，并增加阶段色图例，便于辨认先后顺序。
- 地图增加“显示 / 隐藏节点名称”按钮；默认仅保留编号和节点图标，打开后通过地图内无重叠名称浮层显示全部节点名。
- 天安门城楼在北京景点池提升为 A、评分更新为 91；预订日历新增 8/13 17:00 抢 8/20 城楼票的执行项，并补充官方预约规则。
- 北京代表图固定到正面的真实天安门城楼照片，不再依赖 `Tiananmen Beijing` 的运行时随机搜索结果。
- 首屏四城代表图改用固定缩略图；其他 Commons 查询增加浏览器持久缓存、并发限制和更保守的懒加载距离，减少重复 API 查询与突发请求。
- 威海景点池增加胶东自驾走廊候选：乳山银滩旅游度假区、大乳山滨海旅游度假区、东浦湾 / 逍遥湾、海驴岛、荣成天鹅湖，并明确“进入景点池 ≠ 主推全部去”。

## v1.3.0 — 2026-08-08 11:38 (UTC+8)

- 每一段地点切换新增“距离 + 时长”状态：飞机使用机场间大圆距离；驾车 / 网约车 / 步行 / 公交配置高德后使用实际规划路线数据。
- 铁路增加可选高德 Web Service 跨城查询；具体车次运行时间与票价仍以 12306 为准。
- 刘公岛轮渡显示约 5.6 km 直线水距，并明确区分执行预留时间与官方纯航行时间。
- A–E 五方案每天增加内嵌高德路线图；跨城日拆成出发地本地路线、长途转场卡、到达地本地路线，避免全国尺度挤压市内节点。
- 高德 Key / securityJsCode / Web Service Key 不写入仓库，仅保存在当前浏览器 localStorage；增加 30 天路线缓存和本站客户端每日未缓存算路上限。
- 明确客户端上限不是高德账单硬上限，不把“Key 泄漏也不会扣费”作为安全假设。
- 方案速览增加四城真实代表图；景点池和美食表增加 Wikimedia Commons 真实缩略图，按需懒加载并显示来源 / 许可，失败时回退图标。
- 保留 v1.2.2 的可折叠方案总流程、逐节点日期 / 城市、景点 100 分评分、全部景点总表和四城分表。

## v1.2.2 — 2026-08-08 11:25 (UTC+8)

- 方案总流程改为可折叠，并改为按天摘要，不再堆叠 40+ 节点。
- 每个执行节点新增日期与所在城市信息。
- 新增轻量四城识别图与景点缩略图。

## v1.2.1 — 2026-08-08 10:18 (UTC+8)

- 将方案总路线与 40 个逐日路线展示从经纬度粗投影图改为手机友好的执行流程图，解决长标签互相遮盖、跨城距离压缩市内节点的问题。
- 跨城日重点展示交通接驳、关键时间和转场关系；市内 / 近郊日重点展示节点顺序和节奏。
- 景点池新增统一 100 分评分模型。
- 新增北京 / 青岛 / 威海 / 上海全部景点总表。
- 四城景点分表按“优先级 → 评分”重新排序，并显示城市内排名。
- 保留通用简评、本次旅行判断、预约 / 票价、暑期人流、商业化等原有维度。

## v1.2.0 — 2026-08-08 09:14 (UTC+8)

- 将页面从单一长页导航重构为“方案速览 / 执行行程 / 完整研究”三套独立界面。
- 增加底部“总览 / 每天 / 预订 / 研究”导航。
- 在方案速览选定 A–E 后，执行行程只显示当前方案。
- 路线图默认改为不依赖 OpenStreetMap 的纯前端 SVG 示意图，提升中国大陆网络可访问性。
- 修复逐日路线图错误嵌套到某一天详情下的问题。
- 完整研究目录改为带序号的两列布局。
- 抢票清单新增天安门城楼、国家博物馆、天坛、颐和园、北海公园、景山公园，并补官网 / App / 微信小程序字段。
- 五方案成本新增“不含大交通的预估参考”；原精确账单计算器继续保留，二者明确分离。
- 景点池新增“通用简评”维度。

## v1.1.0 — 2026-08-08 01:07:07 (UTC+8)

- 扩充四城 50+ 景点点评、优先级、暑期人流 / 商业化、预约 / 价格口径。
- 新增 5 套方案总地图 + 40 张方案日地图。
- 新增抢票预约日历、逐餐建议、城市美食与餐厅候选。
- 新增精确成本计算器和五方案成本结构比较。
- “完整研究”升级为多层折叠目录。
- GitHub Pages / Jekyll 采用分片构建，避免超大 HTML 经连接器写入时被截断。

## v1.0.0 — 2026-08-07 22:44 (UTC+8)

- 首次完成 5 份输入方案去重。
- 裁决刘公岛、天安门预约、一嗨异地费、铁路未开售票价、上海双机场等事实冲突。
- 建立五方案骨架、基础完整研究和逐条来源链接。
